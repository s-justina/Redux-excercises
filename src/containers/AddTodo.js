import React from 'react';

const AddTodo = ()=>{
    return(
        <div>
            <input type="text"/>
            <button type='submit'>Add new task</button>
        </div>
    )
};

export default AddTodo;